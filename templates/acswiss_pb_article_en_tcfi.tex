%%%
%%% This is a _DocPu_ template file
%%% modified for ART+COM (v0.4.6)
%%%
%%% *document class:* article, a4paper, 10pt, twoside
%%% *language:* german
%%% *contains:* titlepage, table of contents, list of figures, index
%%% *special:* enable code line numbers, set default image resolution to 150 DPI
%%%

% using article default size
\documentclass[a4paper,10pt,twoside]{article}

% acswiss
\usepackage[T1]{fontenc}
% \usepackage[ngerman]{babel}
\usepackage[utf8]{inputenc}
% \usepackage[standard-baselineskips]{cmbright}
\usepackage[sf]{acswiss}

\usepackage{textcomp}
\usepackage{relsize}
\usepackage{tabls}
\extrarulesep=2pt

% textile required packages
\usepackage{listings}
\usepackage[normalem]{ulem}
\usepackage{graphicx}
\usepackage{float}
\usepackage{makeidx}
\usepackage{hyperref}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{tabulary}


\DeclareUnicodeCharacter{03BC}{\textmu}
\DeclareUnicodeCharacter{00B0}{\textdegree}
\DeclareUnicodeCharacter{00B1}{\textpm}
\DeclareUnicodeCharacter{2610}{\Square}

% pagebreak before section
\newcommand{\sectionbreak}{\clearpage}

\oddsidemargin  -0.04 cm
\evensidemargin -0.04 cm
\textwidth 16 cm
\lstset{
        basicstyle=\ttfamily,
        numberstyle=\tiny,
}
\pagestyle{fancy}
\fancyhead[LE,RO]{\slshape ART+COM AG}
\fancyhead[LO,RE]{\slshape \rightmark}
\fancyfoot[C]{Seite \thepage}

% enable code line numbers
% \lstset{numbers=left}

% enable line break
\lstset{breaklines=true}

% set default image resolution to 150 DPI
\pdfimageresolution 150

% titlepage macros
\title{}
\author{}
\date{\today}

% initialize makeindex
\makeindex

%
\makeatletter
\def\@setref#1#2#3{% csname, extract group, refname
  \ifx#1\relax
  \@bsphack
    \protect\G@refundefinedtrue
%    \nfss@text{\reset@font\bfseries ??}%
    \@latex@warning{%
      Reference `#3' on page \thepage \space undefined%
    }%
    \@esphack
  \else
    \expandafter\Hy@setref@link#1\@empty\@empty\@nil{#2}%
    \fi
}
\renewcommand*\l@section[2]{%
  \ifnum \c@tocdepth >\z@
    \addpenalty\@secpenalty
    \addvspace{1.0em \@plus\p@}%
    \setlength\@tempdima{2em}%
    \begingroup
      \parindent \z@ \rightskip \@pnumwidth
      \parfillskip -\@pnumwidth
      \leavevmode \bfseries
      \advance\leftskip\@tempdima
      \hskip -\leftskip
      #1\nobreak\hfil \nobreak\hb@xt@\@pnumwidth{\hss #2}\par
    \endgroup
    \fi}
  \renewcommand*\l@subsection{\@dottedtocline{2}{2em}{3em}}
  \renewcommand*\l@subsubsection{\@dottedtocline{3}{3.8em}{3.8em}}
\makeatother

\begin{document}
        % title on the first page
        \maketitle

        % table of contents
        \tableofcontents
\newpage
        % list of figures, we don't need it
        % \listoffigures

        % input
        \input{document.tex}

        % print index, we don't need it
%       \printindex
        
\end{document}

